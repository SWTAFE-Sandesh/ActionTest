name: Deploy to IIS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20' # Specify the Node.js version

      - name: Install dependencies
        run: |
          npm install

      - name: Build React App
        run: |
          npm run build

      # Set up the PATH for MSDeploy
      - name: Add MSDeploy to PATH
        run: |
          echo "C:\Program Files\IIS\Microsoft Web Deploy V3" >> $GITHUB_PATH

      # Optional: Check PATH to verify if MSDeploy path is set
      - name: Check PATH
        run: |
          echo $Env:PATH

      - name: Package Application
        run: |
          mkdir output
          powershell -Command "Compress-Archive -Path ./build/* -DestinationPath ./output/ReactApp.zip"

      - name: Deploy to IIS
        env:
          IIS_USERNAME: ${{ secrets.IIS_USERNAME }}
          IIS_PASSWORD: ${{ secrets.IIS_PASSWORD }}
          IIS_SERVER: ${{ secrets.IIS_SERVER }}
          DESTINATION_PATH: ${{ secrets.IIS_SITE }}  # Assuming you have set IIS_SITE as the destination path
        run: |
          echo "Deploying to IIS server..."
          # Use PowerShell to copy files to the IIS server
          pwsh -Command "
          $securePassword = ConvertTo-SecureString $env:IIS_PASSWORD -AsPlainText -Force
          $credential = New-Object System.Management.Automation.PSCredential($env:IIS_USERNAME, $securePassword)
          $session = New-PSSession -ComputerName $env:IIS_SERVER -Credential $credential
          
          # Copy the packaged application to the destination path on the IIS server
          Copy-Item -Path './output/ReactApp.zip' -Destination $env:DESTINATION_PATH -ToSession $session

          # Remove the session
          Remove-PSSession -Session $session
          "
